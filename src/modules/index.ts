// Modules will apply at "host" level

// Getter  - download anything to the host (http relay)
// Runner  - run command and/or script on host
// Grabber - save/upload anything from the host (http post / scp)
// Scanner - run scan against host (port scan, dirb, other?)
// Watcher - watch files/folder on host for any changes (can trigger "Grabber"?)
